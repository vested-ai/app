import PersonaSetup from "@/screens/PersonaSetup";

/**
 * Renders the PersonaSetup component.
 *
 * This routing component returns a JSX element displaying the PersonaSetup screen,
 * which is used for setting up user personas.
 */
export default function PersonaSetupRoute() {
    return (
        <PersonaSetup />
    );
}